apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-nginx
spec:
  type: {{ .Values.service.type }}

  selector:
    app: {{ .Values.name }}-nginx

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80